{{- if and .Values.llmux.otel.enabled }}
# ServiceMonitor for Prometheus Operator
# Uncomment if using Prometheus Operator for metrics collection
# apiVersion: monitoring.coreos.com/v1
# kind: ServiceMonitor
# metadata:
#   name: {{ include "llmux.fullname" . }}
#   labels:
#     {{- include "llmux.labels" . | nindent 4 }}
# spec:
#   endpoints:
#     - port: http
#       path: /metrics
#       interval: 30s
#   selector:
#     matchLabels:
#       {{- include "llmux.selectorLabels" . | nindent 6 }}
{{- end }}
