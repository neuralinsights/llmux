# LLMux Environment Configuration
# Copy this file to .env and modify as needed

# ============ Basic Configuration ============
PORT=8765
TZ=UTC
CONTAINER_NAME=llmux

# ============ Provider Configuration ============
DEFAULT_PROVIDER=claude
REQUEST_TIMEOUT=120000

# Ollama Host Address
# Docker Desktop (Mac/Windows): http://host.docker.internal:11434
# Linux Docker: http://172.17.0.1:11434
# Same compose network: http://ollama:11434
OLLAMA_HOST=http://host.docker.internal:11434

# ============ Cache Configuration ============
CACHE_TTL=3600000
CACHE_MAX_SIZE=1000

# ============ API Key Authentication (Optional) ============
API_KEY_REQUIRED=false
API_KEY=
ADMIN_KEY=

# ============ CLI Session Paths (Optional Override) ============
# Defaults to ~/.claude, ~/.codex, etc.
# CLAUDE_SESSION=/path/to/claude/session
# CODEX_SESSION=/path/to/codex/session
# GEMINI_SESSION=/path/to/gemini/session
# GCLOUD_CONFIG=/path/to/gcloud/config
